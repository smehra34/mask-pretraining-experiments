schema_version: 1
recipe: ../../recipes/1b_llama.yaml
study: 1b-masking-ablation
name: debug-span
description: Span-masking condition with one augmented sequence printed per job.
overrides:
  INPUT_MASK_RATIO: 0.20
  INPUT_MASK_STRATEGY: span
  INPUT_MASK_SPAN_LENGTH: 2
  INPUT_MASK_DEBUG: true
  INPUT_MASK_DEBUG_TOKENS: 64
